<%= t("devise.mailer.invitation_instructions.hello", email: @resource.email)%>

<%= t("devise.mailer.invitation_instructions.someone_invited_you", url: root_url) %>

<%= Rails.application.routes.url_helpers.accept_user_invitation_url(invitation_token: @token, host: root_url) %>

<% if @resource.invitation_due_at %>
  <%= t("devise.mailer.invitation_instructions.accept_until", due_date: l(@resource.invitation_due_at, format: :'devise.mailer.invitation_instructions.accept_until_format')) %>
<% end %>

<%= ActionController::Base.helpers.strip_tags t("devise.mailer.invitation_instructions.ignore") %>
